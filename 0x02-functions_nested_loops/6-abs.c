#include "main.h"
/**
 * _abs - complete absolute value of integers
 * @t: integer to be completed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
